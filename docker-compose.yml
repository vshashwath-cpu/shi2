version: '3.8'

services:
  geoai-cadastre:
    build: .
    container_name: geoai-cadastre-workstation
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PORT=5000
    volumes:
      - ./data:/app/data
